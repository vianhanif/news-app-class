  <% if defined? type %>
    <%=
      case type
      when 'search'
          render 'components/form/search', width: width, name: name, caption: caption, placeholder: (placeholder if defined? placeholder), search: search
        when 'date'
          render 'components/form/date', width: width, form: form, name: name, caption: caption
        when 'date-old'
          render 'components/form/date_old', width: width, form: form, name: name, caption: caption
        when 'money'
          render 'components/form/money',  width: width, form: form, name: name, caption: caption
        when 'textarea'
          render 'components/form/textarea', width: width, form: form, name: name, caption: caption
        when 'editor'
          render 'components/form/ckeditor', width: width, form: form, name: name, caption: caption
        when 'number'
          render 'components/form/number', width: width, form: form, name: name, caption: caption
        when 'checkbox'
          render 'components/form/checkbox', width: width, form: form, name: name, caption: caption
        when 'select'
          render 'components/form/select', width: width, form: form, name: name, caption: caption, select: select, default: (default if defined? default), value: (value if defined? value), selected: (selected if defined? selected), disabled: (disabled if defined? disabled)
        when 'image'
          render 'components/form/image', width: width, form: form, name: name, caption: caption, data: data
        when 'image-primary'
          render 'components/form/imageprimary', width: width, form: form, name: name, caption: caption, data: data
        when 'star'
          render 'components/form/star', width: width, form: form, name: name, caption: caption
        end
    %>
  <% else %>
    <%= render 'components/form/text', width: width, form: form, name: name, caption: caption, placeholder: (placeholder if defined? placeholder), value: (value if defined? value)  %>
  <% end %>
